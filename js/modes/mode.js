export default function Mode() {
  this.internObj = null
  this.optionPanel = null
}

Mode.prototype.onModeChange = function(program, graph, events) {}

Mode.prototype.onMousePressed = function(program, graph, events) {}

Mode.prototype.onKeyPressed = function(program, graph, events) {}

Mode.prototype.onCanvasUpdate = function(program, graph, events) {}

Mode.prototype.onMouseDragged = function(program, graph, events) {}

Mode.prototype.onMouseReleased = function(program, graph, events) {}
