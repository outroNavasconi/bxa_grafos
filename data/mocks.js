import Graph from '../js/graph/graph.js'
import { getDefaultNodeStyle } from '../js/graph/styles/styles.js'
import { getDefaultEdgeStyle } from '../js/graph/styles/styles.js'

export function getSimpleGraph() {
  const graph = new Graph()
  const n1 = graph.addNode(625, 250, getDefaultNodeStyle())
  const n2 = graph.addNode(700, 200, getDefaultNodeStyle())
  const n3 = graph.addNode(775, 250, getDefaultNodeStyle())
  const n4 = graph.addNode(850, 200, getDefaultNodeStyle())
  const n5 = graph.addNode(850, 400, getDefaultNodeStyle())
  const n6 = graph.addNode(700, 400, getDefaultNodeStyle())
  const n7 = graph.addNode(400, 400, getDefaultNodeStyle())
  const n8 = graph.addNode(475, 250, getDefaultNodeStyle())
  const n9 = graph.addNode(400, 200, getDefaultNodeStyle())
  const n10 = graph.addNode(550, 200, getDefaultNodeStyle())
  const e1 = graph.connect(n1, n2, getDefaultEdgeStyle(), false)
  const e2 = graph.connect(n1, n3, getDefaultEdgeStyle(), false)
  const e3 = graph.connect(n1, n6, getDefaultEdgeStyle(), false)
  const e4 = graph.connect(n1, n8, getDefaultEdgeStyle(), false)
  const e5 = graph.connect(n2, n10, getDefaultEdgeStyle(), false)
  const e6 = graph.connect(n2, n4, getDefaultEdgeStyle(), false)
  const e7 = graph.connect(n2, n3, getDefaultEdgeStyle(), false)
  const e8 = graph.connect(n3, n5, getDefaultEdgeStyle(), false)
  const e9 = graph.connect(n3, n6, getDefaultEdgeStyle(), false)
  const e10 = graph.connect(n4, n5, getDefaultEdgeStyle(), false)
  const e11 = graph.connect(n5, n6, getDefaultEdgeStyle(), false)
  const e12 = graph.connect(n6, n8, getDefaultEdgeStyle(), false)
  const e13 = graph.connect(n6, n7, getDefaultEdgeStyle(), false)
  const e14 = graph.connect(n7, n9, getDefaultEdgeStyle(), false)
  const e15 = graph.connect(n8, n9, getDefaultEdgeStyle(), false)
  const e16 = graph.connect(n8, n10, getDefaultEdgeStyle(), false)
  const e17 = graph.connect(n9, n10, getDefaultEdgeStyle(), false)
  e1.setValue(8)
  e2.setValue(13)
  e3.setValue(14)
  e4.setValue(8)
  e5.setValue(11)
  e6.setValue(12)
  e7.setValue(9)
  e8.setValue(13)
  e9.setValue(15)
  e10.setValue(19)
  e11.setValue(15)
  e12.setValue(18)
  e13.setValue(22)
  e14.setValue(21)
  e15.setValue(10)
  e16.setValue(8)
  e17.setValue(12)
  return graph
}